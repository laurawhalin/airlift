<!DOCTYPE html>
<html>
<head>
  <title>DinnerDash</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">
  <link href='http://fonts.googleapis.com/css?family=Galindo' rel='stylesheet' type='text/css'>
  <%= csrf_meta_tags %>

  <meta charset=utf-8 />
  <title>A simple map</title>
  <meta name='viewport' content='initial-scale=1,maximum-scale=1,user-scalable=no' />
  <script src='https://api.tiles.mapbox.com/mapbox.js/v2.1.5/mapbox.js'></script>
  <link href='https://api.tiles.mapbox.com/mapbox.js/v2.1.5/mapbox.css' rel='stylesheet' />
  <style>
  body { margin:0; padding:0; }
  #map { position:absolute; left: -1px; top: 150px; bottom:0; width:100%; }
  </style>

</head>
<body>

  <div class="container">
    <div class="row">

      <div id="restaurant_name" class="col-md-5">
        <h1><%= link_to("Burrito Bombers", root_url )%></h1>

        <% flash.each do |name, msg| %>
          <%= content_tag :div, msg, :id => "flash_#{name}" %>
        <% end %>
      </div>

      <div id="logo" class="col-md-2">
        <%= image_tag "logo.png" %>
      </div>

      <div id="user_nav" class="col-md-5 text-right">
        <div class="row">
          <% if current_user %>
            <div class="col-sm-8">
              <h4>Welcome, <%= current_user.fullname %></h4>
            </div>
            <div class="col-sm-4">
              <%= link_to('Logout', logout_path, :method => :delete, class: "btn btn-default logout") %>
            </div>
          <% else %>
            <%= render partial: "./partials/login" %>
            <%= link_to('Register', register_path) %>
          <% end %>
        </div>
        <div id="cart" class="col-md-2 text-right">
          <i class="fa fa-shopping-cart fa-2x"></i><%= link_to @cart.count_total, cart_path(cart_id = @cart) %>
        </div>
      </div>
  </div>

  <div class="row">
    <div id="category-nav" class="col-md-12">
      <%= render partial: './partials/category_bar' %>
    </div>
  </div>

  <div class="row">
    <%= yield %>
  </div>

<%= debug(params) if Rails.env.development? %>

<%= javascript_include_tag 'application' %>

</body>
<footer>
  <%= link_to "Admin Dashboard", admin_dashboard_index_path if is_admin? %>
</footer>
</html>
