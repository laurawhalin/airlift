<table>
  <% @items.each do |item| %>
  <tr>
    <td>
      <%= item.title %>
    </td>
    <td>
      <%= item.description %>
    </td>
    <td>
      <%= item.price %>
    </td>
    <td>
      <%= button_to "Add to Cart", cart_index_path(item_id: item) %>
    </td>
  </tr>
  <% end %>
</table>
